>  [🏠](../) [⬅️ ০১। লিনাক্স পরিচিতি](../০১-লিনাক্স-পরিচিতি)  [➡️ ০২। শেল এক্সপ্যানশন](../০২-শেল-এক্সপ্যানশন)


# ০১.১। লিনাক্স ইউজার ম্যানেজমেন্ট

> **এই অধ্যায়ে যা শিখবেন:** লিনাক্স একটি মাল্টি-ইউজার সিস্টেম। এই অধ্যায়ে আমরা ইউজারের প্রকারভেদ থেকে শুরু করে ইউজার তৈরি, কনফিগার, সুরক্ষা, পারমিশন, sudo এবং বিশেষ পারমিশন বিট — সবকিছু ধাপে ধাপে হাতে-কলমে শিখবো।


## সূচিপত্র

1. [ইউজারের প্রকারভেদ](#১-লিনাক্সে-ইউজারের-প্রকারভেদ)
2. [গ্রুপ কী?](#২-গ্রুপ-কী)
3. [গুরুত্বপূর্ণ কনফিগারেশন ফাইল](#৩-ইউজার-ম্যানেজমেন্টের-গুরুত্বপূর্ণ-ফাইলসমূহ)
4. [জরুরি কমান্ডসমূহ](#৪-জরুরি-কমান্ডসমূহ)
5. [ইউজার লাইফসাইকেল — ধাপে ধাপে ওয়াকথ্রু](#৫-ইউজার-লাইফসাইকেল--ধাপে-ধাপে-ওয়াকথ্রু)
6. [ইউজার পারমিশন (chmod, chown)](#৬-ইউজার-পারমিশন-chmod-chown)
7. [sudo প্রিভিলেজ দেওয়া](#৭-একজন-ইউজারকে-sudo-প্রিভিলেজ-দেওয়া)
8. [পাসওয়ার্ড এজিং ও chage](#৮-পাসওয়ার্ড-এজিং-ও-chage-কমান্ড)
9. [su বনাম sudo](#৯-su-বনাম-sudo)
10. [বিশেষ পারমিশন বিট (SUID, SGID, Sticky Bit)](#১০-বিশেষ-পারমিশন-বিট)
11. [রিয়েল-ওয়ার্ল্ড ওয়ার্কফ্লো ও কমন ভুল](#১১-রিয়েল-ওয়ার্ল্ড-ওয়ার্কফ্লো-ও-কমন-ভুল)
12. [অধ্যায় সারসংক্ষে](#১২-অধ্যায়-সারসংক্ষেপ)

---

## ১. লিনাক্সে ইউজারের প্রকারভেদ

লিনাক্স একটি **মাল্টি-ইউজার** অপারেটিং সিস্টেম — একসাথে একাধিক ব্যবহারকারী কাজ করতে পারেন। প্রতিটি ইউজারকে সিস্টেম চেনে তার **UID** (User ID) দিয়ে, নাম দিয়ে নয়।

| ইউজার টাইপ | UID | বৈশিষ্ট্য |
|------------|-----|-----------|
| **রুট (Root/Superuser)** | 0 | সিস্টেমের অ্যাডমিন; আনলিমিটেড ক্ষমতা |
| **সিস্টেম ইউজার** | 1–999 | সার্ভিস (Apache, MySQL, Nginx) চালানোর জন্য; লগইন নেই |
| **সাধারণ ইউজার** | 1000+ | নিজের হোম ডিরেক্টরিতে সীমাবদ্ধ |

### সিস্টেম ইউজার তৈরি (Advanced)

সার্ভিস চালানোর জন্য ডেডিকেটেড সিস্টেম ইউজার তৈরি করা হয়:

```bash
sudo useradd -r nginx
```

* `-r` → সিস্টেম ইউজার (UID 1–999 রেঞ্জ থেকে)
* হোম ডিরেক্টরি তৈরি হয় না, লগইন করতে পারে না — যেটা নিরাপত্তার জন্যই কাঙ্ক্ষিত

### লিনাক্স সিস্টেমে Root ইউজার এবং Normal ইউজার এর মূল পার্থক্য হলো তাদের কাজের ক্ষমতা বা পারমিশন।

***নিচে এদের প্রধান পার্থক্যগুলো দেওয়া হলো:***

* ক্ষমতা: Root ইউজারের ক্ষমতা অসীম, সে সিস্টেমের যেকোনো ফাইল পরিবর্তন বা ডিলিট করতে পারে। Normal ইউজারের ক্ষমতা সীমিত, সে শুধু নিজের হোম ডিরেক্টরির ফাইল অ্যাক্সেস করতে পারে।

* সিস্টেমের নিরাপত্তা: Root ইউজারের একটি ভুল কমান্ড পুরো সিস্টেম ক্র্যাশ করে দিতে পারে। Normal ইউজারের ভুল কমান্ডে শুধু তার নিজস্ব ফাইলের ক্ষতি হয়, মূল সিস্টেম নিরাপদ থাকে।

* আইডি (UID): Root ইউজারের ইউজার আইডি বা UID সবসময় 0 হয়। Normal ইউজারের UID সাধারণত 1000 বা তার ওপর থেকে শুরু হয়।

* কমান্ড প্রম্পট চিহ্ন: টার্মিনালে Root ইউজারের নামের শেষে # চিহ্ন থাকে। Normal ইউজারের নামের শেষে $ চিহ্ন থাকে।

* সফ্টওয়্যার ইনস্টলেশন: Root ইউজার নতুন কোনো সফটওয়্যার ইনস্টল বা রিমুভ করতে পারে। Normal ইউজার sudo পারমিশন ছাড়া কোনো সফটওয়্যার ইনস্টল করতে পারে না।

### sudo কখন ব্যবহার করতে হয়

লিনাক্সে মূলত এমন সব কাজের আগে sudo ব্যবহার করতে হয়, যা সরাসরি পুরো সিস্টেমের নিরাপত্তা, কনফিগারেশন বা ফাইল স্ট্রাকচারকে প্রভাবিত করে।
সহজ কথায়, নিচের ৫টি প্রধান ক্যাটাগরির কাজের জন্য সাধারণত sudo প্রয়োজন হয়:

* সফটওয়্যার ইনস্টল বা আপডেট: নতুন কোনো প্যাকেজ ইনস্টল, রিমুভ বা সিস্টেম আপডেট করতে (যেমন: sudo apt install, sudo dnf update)।
* ইউজার ও গ্রুপ ম্যানেজমেন্ট: নতুন ইউজার তৈরি, ডিলিট বা কারো পাসওয়ার্ড পরিবর্তন করতে (যেমন: sudo useradd, sudo usermod)।
* সিস্টেম ফাইল পরিবর্তন: লিনাক্সের মূল কনফিগারেশন ফাইল (যা সাধারণত /etc/ ডিরেক্টরিতে থাকে) এডিট করতে (যেমন: sudo nano /etc/fstab)।
* সার্ভিস ও সিস্টেম কন্ট্রোল: কোনো সিস্টেম সার্ভিস চালু, বন্ধ বা রিবুট করতে (যেমন: sudo systemctl start nginx বা sudo reboot)।
* হার্ডওয়্যার ও ডিস্ক ম্যানেজমেন্ট: নতুন হার্ডডিস্ক ফরম্যাট, মাউন্ট বা পার্টিশন তৈরি করতে (যেমন: sudo fdisk, sudo mount)।

---

## ২. গ্রুপ কী?

গ্রুপ হলো **অনেক ইউজারকে একসাথে ম্যানেজ করার ব্যবস্থা**। ৫০ জন ডেভেলপারের প্রত্যেককে আলাদাভাবে পারমিশন দেওয়ার বদলে একটা `developers` গ্রুপে রেখে একবারেই পারমিশন দেওয়া যায়।

দুই ধরনের গ্রুপ আছে:

| ধরন | বর্ণনা |
|------|--------|
| **Primary Group** | ইউজার তৈরির সময় অটোমেটিক তার নামেই একটা গ্রুপ তৈরি হয় (যেমন: `sakil` ইউজারের জন্য `sakil` গ্রুপ) |
| **Secondary Group** | অতিরিক্ত পারমিশনের জন্য ইউজারকে অন্য গ্রুপে যুক্ত করা হয় (যেমন: `sudo`, `developers`) |


লিনাক্স সিস্টেমে একজন ইউজারের দুই ধরনের গ্রুপ থাকতে পারে: **Primary Group (প্রধান গ্রুপ)**` এবং **Secondary Group (সহায়ক বা অতিরিক্ত গ্রুপ)**।

***এদের মূল পার্থক্যগুলো নিচে দেওয়া হলো:***

* গ্রুপের সংখ্যা: একজন ইউজারের শুধুমাত্র একটিই Primary গ্রুপ থাকতে পারে। কিন্তু সে একাধিক Secondary গ্রুপের সদস্য হতে পারে।
* নতুন ফাইল তৈরি: ইউজার যখনই নতুন কোনো ফাইল বা ফোল্ডার তৈরি করে, সেটি অটোমেটিক তার Primary গ্রুপের মালিকানায় চলে যায়। Secondary গ্রুপের ফাইল তৈরিতে সরাসরি কোনো ভূমিকা নেই।
* তৈরির সময়: লিনাক্সে নতুন ইউজার খোলার সাথে সাথেই ইউজারের নামে একটি Primary গ্রুপ নিজে থেকেই তৈরি হয়ে যায়। অন্যদিকে, ফাইল শেয়ারিং বা বিশেষ পারমিশন দেওয়ার জন্য Secondary গ্রুপ অ্যাডমিনকে ম্যানুয়ালি তৈরি করে নিতে হয়।
* মূল উদ্দেশ্য: ইউজারকে সিস্টেমে লগইন করার নূন্যতম অধিকার দেওয়ার জন্য Primary গ্রুপ কাজ করে। আর নির্দিষ্ট কোনো ফোল্ডার বা প্রজেক্টে (যেমন: developers বা docker গ্রুপ) অন্য ইউজারদের সাথে ফাইল শেয়ার করার পারমিশন দিতে Secondary গ্রুপ ব্যবহার করা হয়।



---

## ৩. ইউজার ম্যানেজমেন্টের গুরুত্বপূর্ণ ফাইলসমূহ

লিনাক্সে সব ইউজার ও গ্রুপের তথ্য কিছু নির্দিষ্ট টেক্সট ফাইলে সংরক্ষিত থাকে। **কমান্ড চালালে আসলে এই ফাইলগুলোই পরোক্ষভাবে মডিফাই হয়** — তাই এগুলো বোঝা অপরিহার্য।

| ফাইল | কাজ | পারমিশন |
|------|-----|---------|
| `/etc/passwd` | ইউজারের নাম, UID, হোম ডিরেক্টরি, শেল | সবাই পড়তে পারে (644) |
| `/etc/shadow` | এনক্রিপ্টেড পাসওয়ার্ড ও এজিং তথ্য | শুধু root (000/400) |
| `/etc/group` | গ্রুপের তালিকা ও মেম্বার | সবাই পড়তে পারে (644) |
| `/etc/gshadow` | গ্রুপের সুরক্ষিত পাসওয়ার্ড | শুধু root |

### ৩.১ `/etc/passwd` — ইউজার অ্যাকাউন্ট তথ্য

সবাই এই ফাইল **পড়তে পারে**, কিন্তু লিখতে পারে শুধু root। **৭টি ফিল্ড**, `:` দিয়ে আলাদা:

```text
Ex-1:
  root:x:0:0:root:/root:/bin/bash
    1  2 3 4  5     6     7
Ex-2;
  daemon:x:2:2:daemon:/sbin:/sbin/nologin
    1    2 3 4  5       6     7
```

| ফিল্ড | মান | ব্যাখ্যা |
| ------ | ----- | --------- |
| ১ | `rahim` | ইউজারনেম (লগইন নাম) |
| ২ | `x` | পাসওয়ার্ড placeholder (আসল হ্যাশ `/etc/shadow`-এ) |
| ৩ | `1001` | UID (root=0, system=1-999, normal=1000+) |
| ৪ | `1001` | Primary গ্রুপের GID |
| ৫ | `Rahim Uddin` | GECOS (পুরো নাম/কমেন্ট) |
| ৬ | `/home/rahim` | হোম ডিরেক্টরি |
| ৭ | `/bin/bash` | শেল (`/sbin/nologin` = লগইন বন্ধ) |

### ৩.২ `/etc/shadow` — সুরক্ষিত পাসওয়ার্ড

শুধুমাত্র **root** পড়তে পারে। এখানে hashed পাসওয়ার্ড ও এজিং তথ্য সহ সর্বমোট ৯ টি তথ্য  থাকে :

```text
rahim:$6$K8x...:19700:5:90:7:14:19800:-
  1       2      3    4 5  6  7   8   9
```

| ফিল্ড | মানে |
| ------ | ------ |
| ১ | ইউজারনেম |
| ২ | এনক্রিপ্টেড পাসওয়ার্ড (`$6$`=SHA-512, `!`=লক, `*`=লগইন অসম্ভব) |
| ৩ | শেষ পরিবর্তন (১৯৭০ থেকে দিন গণনা) |
| ৪ | Min days — কমপক্ষে কতদিন পর বদলানো যাবে |
| ৫ | Max days — কতদিনের মধ্যে বদলাতেই হবে |
| ৬ | Warn days — মেয়াদ শেষের আগে সতর্কবার্তা |
| ৭ | Inactive — মেয়াদ শেষে লগইনের সুযোগ |
| ৮ | Expire — অ্যাকাউন্ট বন্ধের তারিখ |
| ৯ | সংরক্ষিত |

Linux সিস্টেমে ইউজার পাসওয়ার্ডের সিকিউরিটি এবং পলিসি ম্যানেজ করার জন্য /etc/shadow ফাইল ব্যবহার করা হয়। আপনার দেওয়া তিনটি লাইন মূলত এই ফাইলেরই তিনটি ভিন্ন ধরণের এন্ট্রি (Entry)।
প্রতিটি লাইন : (ক্লোন) চিহ্ন দিয়ে ৯টি সেগমেন্টে বিভক্ত থাকে। নিচে প্রতিটির বিস্তারিত ব্যাখ্যা দেওয়া হলো:

### প্রতিটি সেগমেন্টের সাধারণ কাঠামো (9 Segments)

***একটি স্ট্যান্ডার্ড শ্যাডো ফাইলের ফরম্যাট এমন হয়:***

>Username : Password : LastChanged : MinDays : MaxDays : WarnDays : InactiveDays : ExpireDate : Reserved

### ১. প্রথম লাইন (সক্রিয় ইউজার এবং পাসওয়ার্ড হ্যাশ)

```text
root:$6$K8MQE6rMeJ8wWEUo$vg3h.rv7kzXdw2vr0Z4IEl440wcC8LIQ/gUHDxs2PuePNLnS90MgnSh:20270:0:99999:7:::
```

* ১ম সেগমেন্ট (root): ইউজারের নাম (Username)। এখানে এটি সুপার-ইউজার বা রুট অ্যাকাউন্ট।
* ২য় সেগমেন্ট ($6$K8MQE6...): এটি এনক্রিপ্টেড পাসওয়ার্ড হ্যাশ। এর আবার ৩টি অংশ আছে:

  * `$6$:` এনক্রিপশন অ্যালগরিদম নির্দেশ করে। 6 মানে SHA-512 (যা অত্যন্ত সুরক্ষিত)।

  * K8MQE6rMeJ8wWEUo: এটিকে Salt বলে। পাসওয়ার্ড ক্র্যাক করা কঠিন করার জন্য এটি একটি এলোমেলো (random) স্ট্রিং।

  * বাকি অংশ ($vg3h...): এটি মূল পাসওয়ার্ডের এনক্রিপ্টেড রূপ।

* ৩য় সেগমেন্ট (20270): ১ জানুয়ারি ১৯৭০ (Epoch time) থেকে কতদিন পর পাসওয়ার্ডটি শেষবার পরিবর্তন করা হয়েছে। ২০২৭০ দিন মানে এটি আনুমানিক জুলাই ২০২৫-এ পরিবর্তন করা হয়েছে।
* ৪র্থ সেগমেন্ট (0): পাসওয়ার্ড পরিবর্তনের সর্বনিম্ন দিন (Minimum days)। 0 মানে ইউজার যেকোনো সময় চাইলে পাসওয়ার্ড পরিবর্তন করতে পারবে, কোনো অপেক্ষা করতে হবে না।
* ৫ম সেগমেন্ট (99999): পাসওয়ার্ডের সর্বোচ্চ মেয়াদ (Maximum days)। 99999 মানে এই পাসওয়ার্ডের মেয়াদ কখনো শেষ হবে না (প্রায় ২৭৩ বছর)।
* ৬ষ্ঠ সেগমেন্ট (7): পাসওয়ার্ডের মেয়াদ শেষ হওয়ার কতদিন আগে ইউজারকে ওয়ার্নিং (Warning) দেখাবে। এখানে মেয়াদ শেষ হওয়ার ৭ দিন আগে সতর্কবার্তা দেবে।
* ৭ম সেগমেন্ট (ফাঁকা ): পাসওয়ার্ডের মেয়াদ শেষ হওয়ার কতদিন পর অ্যাকাউন্টটি নিষ্ক্রিয় (Inactive) হবে। ফাঁকা মানে এই নিয়ম প্রযোজ্য নয়।
* ৮ম সেগমেন্ট (ফাঁকা ): অ্যাকাউন্টটি কোন নির্দিষ্ট তারিখে পুরোপুরি এক্সপায়ার (Expire) বা বন্ধ হয়ে যাবে। ফাঁকা মানে অ্যাকাউন্টটি আজীবন সচল।
* ৯ম সেগমেন্ট (ফাঁকা ): এটি ভবিষ্যৎ ব্যবহারের জন্য সংরক্ষিত (Reserved)।

### ২. দ্বিতীয় লাইন (লকড/সিস্টেম ইউজার)

`daemon:*:19767:0:99999:7:::`

* ১ম সেগমেন্ট (daemon): সিস্টেমের একটি ব্যাকগ্রাউন্ড সার্ভিস ইউজার (System User)।
* ২য় সেগমেন্ট (*): পাসওয়ার্ড ফিল্ডে শুধু একটি তারকা চিহ্ন (*) আছে। এর মানে হলো এই অ্যাকাউন্টটি লক করা (Locked)। কোনো মানুষ এই পাসওয়ার্ড দিয়ে সরাসরি লগইন করতে পারবে না। এটি কেবল সিস্টেমের অভ্যন্তরীণ কাজের জন্য ব্যবহৃত হয়।
* ৩য় সেগমেন্ট (19767): শেষবার পাসওয়ার্ড পরিবর্তনের দিন (Epoch দিন হিসেবে)।
* বাকি সেগমেন্টগুলো `0:99999:7::::` প্রথম লাইনের মতোই সর্বনিম্ন দিন, সর্বোচ্চ দিন এবং ওয়ার্নিং পিরিয়ড নির্দেশ করে।

### ৩. তৃতীয় লাইন (পাসওয়ার্ড বিহীন/ডিজেবল্ড ইউজার)

`sssd:!!:19883::::::`

* ১ম সেগমেন্ট (sssd): System Security Services Daemon-এর জন্য তৈরি সিস্টেম ইউজার।
* ২য় সেগমেন্ট (!!): পাসওয়ার্ড ফিল্ডে দুটি বিস্ময়সূচক চিহ্ন (!!) আছে। এর মানে হলো এই অ্যাকাউন্টের কোনো পাসওয়ার্ড সেট করা হয়নি (No Password Set) এবং অ্যাকাউন্টটি সম্পূর্ণ নিষ্ক্রিয়। কোনোভাবেই এই অ্যাকাউন্ট দিয়ে সরাসরি লগইন সম্ভব নয়।
* ৩য় সেগমেন্ট (19883): অ্যাকাউন্টটি তৈরির দিন বা Epoch দিন।
* বাকি সেগমেন্টগুলো সব ফাঁকা `:` পাসওয়ার্ডের কোনো মেয়াদ বা পলিসি এই অ্যাকাউন্টের জন্য সেট করা নেই, কারণ অ্যাকাউন্টটিতে কোনো পাসওয়ার্ডই নেই।

#### সংক্ষেপে মনে রাখার ট্রিক

   1. পাসওয়ার্ডে বড় টেক্সট থাকলে: সক্রিয় অ্যাকাউন্ট।
   2. পাসওয়ার্ডে * থাকলে: লকড অ্যাকাউন্ট (সিস্টেম ইউজার)।
   3. পাসওয়ার্ডে !! থাকলে: পাসওয়ার্ড সেট করা নেই (ডিজেবল্ড অ্যাকাউন্ট)।

### ৩.৩ `/etc/group` — গ্রুপের তথ্য

```text
developers:x:1002:rahim,karim
```

| ফিল্ড | বর্ণনা |
| --- | --- |
| 1. Group name | গ্রুপের নাম |
| 2. `x` | পাসওয়ার্ড placeholder |
| 3. GID | গ্রুপ ID |
| 4. Members | সেকেন্ডারি গ্রুপ মেম্বার (কমা-সেপারেটেড) |

**নোট:** প্রাইমারি গ্রুপের মেম্বার এখানে দেখা যায় না — তা `/etc/passwd`-এর GID থেকে বোঝা যায়।

### ৩.৪ `/etc/gshadow` — গ্রুপের সুরক্ষিত তথ্য

```text
developers:!::rahim
```

`!` = গ্রুপ পাসওয়ার্ড নেই (ডিফল্ট)। গ্রুপ পাসওয়ার্ড থাকলে অন্য ইউজার `newgrp` দিয়ে গ্রুপে জয়েন করতে পারে।

### ফাইলগুলোর সম্পর্ক

```text
/etc/passwd  ←→  /etc/shadow   (ইউজার লিংক: username)
/etc/group   ←→  /etc/gshadow  (গ্রুপ লিংক: groupname)
```

### দরকারি কমান্ড

```bash
cat /etc/passwd                 # ফাইল দেখা
sudo cat /etc/shadow            # shadow দেখা (root লাগবে)
getent passwd rahim             # নির্দিষ্ট ইউজারের তথ্য
id rahim                        # UID, GID, গ্রুপ দেখা
pwck && grpck                   # ফাইলের ভুল যাচাই
ls -l /etc/passwd /etc/shadow   # পারমিশন দেখা
```

### নিরাপত্তা নোট

* `/etc/shadow` ও `/etc/gshadow` কঠোরভাবে root-সীমিত রাখুন — হ্যাশ লিক হলে **brute-force আক্রমণের ঝুঁকি** তৈরি হয়
* `/etc/passwd` ও `/etc/group` ম্যানুয়ালি এডিট **করবেন না** — এক ক্যারেক্টার ভুলে পুরো সিস্টেমে লগইন ভেঙে যেতে পারে

### ইউজার ডিফল্ট সেটিংস নিয়ন্ত্রণকারী ফাইল (Sysadmin Level):

```bash
/etc/default/useradd   # ডিফল্ট শেল, হোম ডিরেক্টরি লোকেশন
/etc/login.defs        # UID রেঞ্জ, পাসওয়ার্ড এজিং পলিসি
/etc/skel/             # নতুন ইউজারের হোমে যেসব ফাইল (.bashrc ইত্যাদি) যাবে
```

### ৩.৫ লিনাক্স সিস্টেম ও ইউজার-ডিফাইন্ড ইউজার এবং গ্রুপ চেনার উপায়

Linux সিস্টেমে System Built-in Users/Groups (সিস্টেমের নিজস্ব ইউজার/গ্রুপ) এবং User-defined Users/Groups (আপনার তৈরি করা ইউজার/গ্রুপ) চেনার সবচেয়ে সহজ এবং স্ট্যান্ডার্ড উপায় হলো তাদের UID (User ID) এবং GID (Group ID) চেক করা।
লিনাক্স প্রতিটি ইউজারের জন্য একটি ইউনিক আইডি নম্বর দেয়। এই আইডির রেঞ্জ বা সীমা দেখেই বুbuilt-in এবং user-defined এর পার্থক্য বোঝা যায়।

#### ১. UID এবং GID এর আইডি রেঞ্জ (ID Ranges)

CentOS 8 এবং আধুনিক লিনাক্স ডিস্ট্রিবিউশনগুলোতে আইডি সাধারণত এভাবে ভাগ করা থাকে:

| ইউজারের ধরণ | UID / GID রেঞ্জ | উদাহরণ |
| --- | --- | --- |
| Root (সুপার ইউজার) | 0 | root |
| System Built-in | 1 থেকে 999 | bin, daemon, mail, sshd, nginx |
| User-defined (Regular) | 1000 থেকে 60000+ | rony, asif, tisha |

নোট: পুরোনো লินাক্স সিস্টেমে (যেমন CentOS 6) সিস্টেম ইউজারের রেঞ্জ ছিল ১ থেকে ৪৯৯ এবং রেগুলার ইউজার শুরু হতো ৫০০ থেকে। কিন্তু CentOS 7/8 বা উবুন্টুতে এটি ১০০০ থেকে শুরু হয়।

#### ২. ইউজার চেক করার উপায় (Users)

আপনার সিস্টেমের সব ইউজার দেখতে টার্মিনালে রান করুন: cat /etc/passwd। এই ফাইলের ৩ নম্বর সেগমেন্টটি হলো UID।

##### কমান্ডের মাধ্যমে আলাদা করার ট্রিকস

* শুধুমাত্র User-defined (Regular) ইউজারদের দেখতে:
যেহেতু রেগুলার ইউজারদের UID ১০০০ বা তার বেশি হয়, তাই এই কমান্ডটি দিয়ে সরাসরি তাদের ফিল্টার করতে পারেন:

```text
awk -F: '$3 >= 1000 && $3 != 65534 {print $1, $3}' /etc/passwd
```

(এখানে 65534 হলো 'nobody' ইউজারের আইডি, যা একটি সিস্টেম ইউজার, তাই এটিকে বাদ দেওয়া হয়েছে।)

* শুধুমাত্র System Built-in ইউজারদের দেখতে:

```text
awk -F: '$3 > 0 && $3 < 1000 {print $1, $3}' /etc/passwd
```

#### ৩. গ্রুপ চেক করার উপায় (Groups)

ইউজারের মতোই লিনাক্সের সব গ্রুপ থাকে /etc/group ফাইলে। এর ৩ নম্বর সেগমেন্টটি হলো GID।

* শুধুমাত্র User-defined গ্রুপ দেখতে (GID >= 1000):

```text
awk -F: '$3 >= 1000 && $3 != 65534 {print $1, $3}' /etc/group
```

* শুধুমাত্র System Built-in গ্রুপ দেখতে (GID < 1000):

```text
awk -F: '$3 > 0 && $3 < 1000 {print $1, $3}' /etc/group
```

#### ৪. একটি নির্দিষ্ট ইউজার বা গ্রুপ চেক করার শর্টকাট

আপনার কাছে যদি একটি নির্দিষ্ট ইউজারের নাম থাকে (যেমন: sssd বা john) এবং আপনি নিশ্চিত হতে চান সেটি কোন ক্যাটাগরির, তবে জাস্ট id কমান্ডটি লিখুন:

id john

আউটপুট উদাহরণ: `uid=1001(john) gid=1001(john) groups=1001(john)`

যেহেতু আইডি 1001 (১০০০ এর উপরে), তাই এটি একটি User-defined ইউজার।

id sssd

আউটপুট উদাহরণ: `uid=995(sssd) gid=993(sssd) groups=993(sssd)`

যেহেতু আইডি 995 (১০০০ এর নিচে), তাই এটি একটি System Built-in ইউজার।

এই আইডি রেঞ্জের নিয়মটি কি আপনার সার্ভারে ইউজার ম্যানেজমেন্ট করতে সাহায্য করবে? আপনি চাইলে নিচে দেওয়া বিষয়গুলো দেখতে পারেন:

* নতুন ইউজার তৈরি করার সময় নিজের পছন্দমতো UID সেট করার কমান্ড দেখাও
* একটি ইউজার কোন কোন গ্রুপের মেম্বার তা দেখার কমান্ড কী?

---

## ৪. জরুরি কমান্ডসমূহ (Quick Reference)

### ইউজার তৈরি ও ডিলিট:
```bash
sudo useradd username        # নতুন ইউজার (হোম ডিরেক্টরি হয় না!)
sudo passwd username         # পাসওয়ার্ড সেট
sudo userdel username        # ইউজার ডিলিট (হোম থেকে যায়)
sudo userdel -r username     # হোম ডিরেক্টরিসহ ডিলিট ✅
```

### ইউজার মডিফাই:
```bash
sudo usermod -aG sudo rahim      # sudo গ্রুপে যোগ
sudo usermod -l newname oldname  # নাম পরিবর্তন (UID একই থাকে)
sudo usermod -s /bin/zsh rahim   # শেল পরিবর্তন
sudo usermod -d /home/newdir -m rahim  # হোম ডিরেক্টরি সরানো
```

### গ্রুপ ম্যানেজমেন্ট:
```bash
sudo groupadd groupname      # গ্রুপ তৈরি
sudo groupdel groupname      # গ্রুপ ডিলিট
```

### ইউজার চেক:
```bash
whoami                       # বর্তমান ইউজার
id username                  # UID, GID, গ্রুপ
groups username              # গ্রুপ তালিকা
```

---

## ৫. ইউজার লাইফসাইকেল — ধাপে ধাপে ওয়াকথ্রু

এখন আমরা একটা ইউজারের **সম্পূর্ণ জীবনচক্র** হাতে-কলমে করবো:

> **তৈরি → কনফিগার → পরিবর্তন → সুরক্ষা → মুছে ফেলা**

### ধাপ ১: ইউজার তৈরি

**সাধারণ কমান্ড (অসম্পূর্ণ!):**

```bash
useradd sakil
```

**ভেতরে যা ঘটে:**
* ✅ `/etc/passwd` ও `/etc/shadow`-এ এন্ট্রি যোগ হয়
* ❌ হোম ডিরেক্টরি **তৈরি হয় না**
* ❌ ডিফল্ট শেল প্রায়ই `/bin/sh` (অসুবিধাজনক)

**সঠিক/প্রোডাকশন-রেডি উপায়:**

```bash
sudo useradd -m -s /bin/bash -c "Sakil Mahmud" sakil
```

| ফ্ল্যাগ | কাজ |
|--------|-----|
| `-m` | `/home/sakil` হোম ডিরেক্টরি তৈরি (skel ফাইলসহ) |
| `-s /bin/bash` | শেল অ্যাসাইন |
| `-c` | GECOS ফিল্ডে পুরো নাম |

**ভেরিফাই:**

```bash
id sakil
# uid=1001(sakil) gid=1001(sakil) groups=1001(sakil)
```

### ধাপ ২: পাসওয়ার্ড সেট

নতুন ইউজার **লক অবস্থায়** থাকে — পাসওয়ার্ড সেট না করা পর্যন্ত লগইন অসম্ভব:

```bash
sudo passwd sakil
```

* পাসওয়ার্ড **হ্যাশ হয়ে** `/etc/shadow`-এ যায়
* টাইপ করার সময় **কিছুই দেখাবে না** — এটা স্বাভাবিক ✅

### ধাপ ৩: গ্রুপে যোগ করা

```bash
sudo usermod -aG developers sakil
```

⚠️ **সবচেয়ে গুরুত্বপূর্ণ সতর্কতা:**
* `-a` = append (যোগ) — **এটা ভুললেই বিপদ!**
* শুধু `-G` লিখলে আগের **সব সেকেন্ডারি গ্রুপ মুছে যায়**

```bash
groups sakil   # ভেরিফাই
```

### ধাপ ৪: প্রপার্টি পরিবর্তন

```bash
sudo usermod -l newname sakil              # নাম বদল (UID অপরিবর্তিত)
sudo usermod -d /home/newdir -m sakil      # হোম বদল (-m = ফাইল সরাও)
sudo usermod -s /bin/zsh sakil             # শেল বদল
sudo usermod -g developers sakil           # প্রাইমারি গ্রুপ বদল (ছোট -g)
```

### ধাপ ৫: লক / আনলক

```bash
sudo passwd -l sakil   # লক → shadow-তে হ্যাশের আগে '!' বসে
sudo passwd -u sakil   # আনলক
```

👉 ইউজার ডিলিট না করে সাময়িকভাবে বন্ধ করার সেরা উপায়।

### ধাপ ৬: এক্সপায়ার / ডিজেবল

```bash
sudo passwd -e sakil              # পরের লগইনে পাসওয়ার্ড বদলাতে বাধ্য
sudo chage -E 2026-12-31 sakil    # নির্দিষ্ট তারিখে অ্যাকাউন্ট বন্ধ
sudo chage -l sakil               # এজিং তথ্য দেখা
```

বিস্তারিত `chage` → [সেকশন ৮](#৮-পাসওয়ার্ড-এজিং-ও-chage-কমান্ড)

### ধাপ ৭: ইউজার সুইচ (টেস্টিং)

```bash
su - sakil
```

* `-` (হাইফেন) → সম্পূর্ণ এনভায়রনমেন্ট লোড হয়
* হাইফেন ছাড়া `su sakil` দিলে PATH/হোম ঠিকমতো লোড হয় না — **কমন ভুল!**

### ধাপ ৮: ডিলিট

```bash
sudo userdel sakil      # হোম ডিরেক্টরি থেকে যায় ❌
sudo userdel -r sakil   # হোম + মেইল স্পুলসহ সম্পূর্ণ ক্লিনআপ ✅
```

⚠️ ডিলিটের আগে গুরুত্বপূর্ণ ফাইল **ব্যাকআপ** নিন!

---

## ৬. ইউজার পারমিশন (chmod, chown)

### পারমিশনের প্রকার:

| পারমিশন | প্রতীক | সংখ্যা | ফাইলে | ডিরেক্টরিতে |
|---------|--------|--------|--------|--------------|
| Read | r | 4 | পড়া | তালিকা দেখা |
| Write | w | 2 | এডিট | তৈরি/ডিলিট |
| Execute | x | 1 | চালানো | প্রবেশ |

### তিন ধরনের ব্যক্তি:
* **u (User)** — মালিক
* **g (Group)** — গ্রুপ মেম্বার
* **o (Others)** — বাকি সবাই

### পারমিশন পড়া:

```
-rwxr-xr-- 1 rahim developers 1024 file.txt
│ └─┬─┘ └─┬─┘ └─┬─┘
│ owner  group others
ফাইল
```

### chmod (Numeric):

```bash
chmod 755 file.txt   # rwxr-xr-x (স্ক্রিপ্টের জন্য)
chmod 644 file.txt   # rw-r--r-- (সাধারণ ফাইল)
chmod 600 file.txt   # rw------- (প্রাইভেট ফাইল, SSH key)
chmod -R 755 folder/ # রিকার্সিভ
```

### chmod (Symbolic):

```bash
chmod u+x script.sh  # মালিকের execute
chmod g-w file.txt   # গ্রুপের write বাদ
chmod o=r file.txt   # অন্যদের শুধু read
```

### chown (মালিকানা):

```bash
sudo chown rahim file.txt              # মালিক বদল
sudo chown rahim:developers file.txt   # মালিক+গ্রুপ
sudo chgrp developers file.txt         # শুধু গ্রুপ
sudo chown -R rahim /path/             # রিকার্সিভ
```

---

## ৭. একজন ইউজারকে sudo প্রিভিলেজ দেওয়া

### পদ্ধতি ১ — গ্রুপে যোগ (সহজ):

```bash
sudo usermod -aG sudo rahim      # Ubuntu/Debian
sudo usermod -aG wheel rahim     # RHEL/CentOS
```

⚠️ আবারও মনে করিয়ে দিলাম: **`-aG`** ব্যবহার করুন — শুধু `-G` দিলে আগের সব সেকেন্ডারি গ্রুপ মুছে যায়!

### পদ্ধতি ২ — sudoers ফাইল:

```bash
sudo visudo          # সরাসরি এডিট করবেন না, অবশ্যই visudo ব্যবহার করুন
```

```
rahim ALL=(ALL:ALL) ALL
```

| অংশ | মানে |
|------|------|
| `rahim` | ইউজারনেম |
| `ALL=` | যেকোনো হোস্ট |
| `(ALL:ALL)` | যেকোনো ইউজার/গ্রুপ হিসেবে |
| `ALL` | যেকোনো কমান্ড |

### সীমিত অনুমতি (নিরাপদ):

```
rahim ALL=(ALL) NOPASSWD: /usr/bin/apt update
```

### যাচাই ও বাতিল:

```bash
sudo -l -U rahim           # অনুমতি দেখা
sudo deluser rahim sudo    # sudo বাতিল
```

---

## ৮. পাসওয়ার্ড এজিং ও chage কমান্ড

`/etc/shadow`-এর ফিল্ডগুলো (সেকশন ৩.২ দেখুন) ম্যানেজ করার সেরা টুল হলো `chage` — সরাসরি ফাইল এডিট করার দরকার নেই।

```bash
sudo chage -l rahim              # সেটিংস দেখা
sudo chage -m 5 rahim            # Min days
sudo chage -M 90 rahim           # Max days
sudo chage -W 7 rahim            # সতর্কবার্তা
sudo chage -I 14 rahim           # Inactive
sudo chage -E 2024-12-31 rahim   # অ্যাকাউন্ট এক্সপায়ারি
sudo chage -E -1 rahim           # এক্সপায়ারি বাতিল
sudo chage -d 0 rahim            # প্রথম লগইনেই পাসওয়ার্ড বদলাতে বাধ্য ⭐
sudo chage -m 5 -M 90 -W 7 -I 14 rahim   # একসাথে সব
```

👉 **ব্যবহারিক টিপ:** নতুন ইউজার বানানোর পর `chage -d 0` দিন — প্রথমবার লগইন করে নিজের পাসওয়ার্ড সেট করতে বাধ্য হবে। কন্ট্রাক্ট কর্মী/ইন্টার্নদের জন্য `-E` দিয়ে মেয়াদ ঠিক করে িন।

---

## ৯. su বনাম sudo

| বিষয় | `su` | `sudo` |
|------|------|--------|
| পাসওয়ার্ড | রুটের | নিজের |
| সুযোগ | পুরো সেশন | একটি কমান্ড |
| লগ | ট্রেস কঠিন | auth.log-এ রেকর্ড |
| নিরাপত্তা | কম | বেশি (অডিট + সীমিত) |

### su ব্যবহার:

```bash
su -          # রুট সেশন (সম্পূর্ণ পরিবেশসহ)
su - rahim    # রহিম হিসেবে সুইচ
```

⚠️ সবসময় `su -` (হাইফেন **সহ**) ব্যবহার করুন — হাইফেন ছাড়া দিলে এনভায়রনমেন্ট (PATH, HOME) ঠিকমতো লোড হয় না,যার ফলে অদ্ভুত এরর দেখা দিতে পারে।

### sudo-এর দরকারি অপশন:

```bash
sudo -i            # রুটের সম্পূর্ণ লগইন শেল
sudo -s            # রুট শেল
sudo -u rahim ls   # রহিম হিসেবে কমান্ড চালানো
sudo !!            # শেষ কমান্ডটি sudo সহ আবার চালানো ⭐
sudo -k            # টাইমস্ট্যাম্প রিসেট (পরেরবার আবার পাসওয়ার্ড লাগবে)
```

📌 sudo পাসওয়ার্ড ডিফল্টভাবে **১৫ মিনিট** মনে রাখে — এর মধ্যে বারবার পাসওয়ার্ড দিতে হয় না।

### 🏆 শ্রেষ্ঠ অভ্যাস (Best Practice):

* রুট লগইন বন্ধ রাখুন: `sudo passwd -l root`
* সরাসরি root ব্যবহার না করে সবাইকে **sudo** দিন — কে কখন কী করেছে তা `auth.log`-এ রেকর্ড হয় (অডিট ট্রেইল)

---

## ১০. বিশেষ পারমিশন বিট (SUID, SGID, Sticky Bit)

সাধারণ `rwx` পারমিশনের বাইরে আরও তিনটি **বিশেষ বিট** আছে:

```
-rwsr-sr-t
   │   │  └─ t = Sticky Bit
   │   └──── s = SGID
   └──────── s = SUID
```

### 🔴 SUID (4)

ফাইল চালালে চালানো ব্যক্তি **মালিকের (সাধারণত রুটের)** ক্ষমতা পায়।

```bash
ls -l /usr/bin/passwd
-rwsr-xr-x 1 root root ... /usr/bin/passwd
```

👉 এ কারণেই সাধারণ ইউজার `passwd` কমান্ড চালিয়ে নিজের পাসওয়ার্ড বদলাতে পারে — অথচ `/etc/shadow` ফাইল তার পড়ারও অনুমতি নেই! কমান্ডটি চলার সময় **root-এর ক্ষমতায়** চলে।

```bash
chmod u+s file    # বা chmod 4755 file
find / -perm -4000 -type f 2>/dev/null   # সিস্টেমের সব SUID ফাইল খোঁজা (অডিট)
```

### 🟡 SGID (2)

* **ফাইলে:** চালালে ফাইলের **গ্রুপের** ক্ষমতা পায়
* **ডিরেক্টরিতে:** ভেতরে তৈরি হওয়া **নতুন ফাইল অটোমেটিক সেই ডিরেক্টরির গ্রুপের হয়ে যায়** — টিম ফোল্ডারে দারুণ কাজে লাগে!

```bash
sudo groupadd developers
sudo mkdir /home/project
sudo chgrp developers /home/project
sudo chmod 2775 /home/project    # drwxrwsr-x
```

### 🔵 Sticky Bit (1)

ডিরেক্টরিতে থাকলে — যে ফাইল **নিজের, শুধু সে-ই সেটা ডিলিট করতে পারবে** (অন্যরা পড়তে/লিখতে পারলেও):

```bash
ls -ld /tmp
drwxrwxrwt 15 root root ... /tmp     # শেষে 't' দেখুন

chmod +t folder/   # বা chmod 1777
```

### সারসংক্ষেপ:

| বিট | ফাইলে | ডিরেক্টরিতে | সংখ্যা | বাস্তব উদাহরণ |
|-----|--------|-------------|--------|---------------|
| SUID | মালিকের ক্ষমতায় চলে | অর্থহীন | 4 | `/usr/bin/passwd` |
| SGID | গ্রুপের ক্ষমতায় চলে | গ্রুপ-উত্তরাধিকার | 2 | টিম ফোল্ডার |
| Sticky | অপ্রচলিত | শুধু নিজের ফাইল ডিলিট | 1 | `/tmp` |

### বড় হাতের বনাম ছোট হাতের অক্ষর (গুরুত্বপূর্ণ!):

* `rws` (ছোট s) → execute আছে, বিশেষ বিট **কার্যকর** ✅
* `rwS` (বড় S) → execute **নেই**, বিট **অকার্যকর** ⚠️
* `rwt` / `rwT` → Sticky Bit-এও একই নিয়ম

```bash
chmod 4754 file   # -rwsr-xr-- ✅ (কাজ করবে)
chmod 4644 file   # -rwSr--r-- ❌ (কাজ করবে না)
```

---

## ১১. রিয়েল-ওয়ার্ল্ড ওয়ার্কফ্লো ও কমন ভুল

### 🏢 প্রোডাকশন-রেডি ইউজার সেটআপ (সম্পূর্ণ উদাহরণ)

ধরুন, নতুন একজন ডেভেলপার "সাকিল" জয়েন করলো। আপনার করণীয়:

**ধাপ ১ — গ্রুপ তৈরি:**

```bash
sudo groupadd developers
```

**ধাপ ২ — ইউজার তৈরি (গ্রুপসহ একসাথে):**

```bash
sudo useradd -m -s /bin/bash -c "Sakil Mahmud" -G developers sakil
```

**ধাপ ৩ — পাসওয়ার্ড সেট + প্রথম লগইনে বদলাতে বাধ্য করা:**

```bash
sudo passwd sakil
sudo chage -d 0 sakil
`

**ধাপ ৪ — sudo অ্যাক্সেস দেওয়া:**

```bash
sudo usermod -aG sudo sakil
```

**ধাপ ৫ — টিম ফোল্ডারে অ্যাক্সেস দেওয়া (SGID ব্যবহার করে):**

```bash
sudo chmod 2775 /home/project
```

**ধাপ ৬ — সবকিছু ভেরিফাই:**

```bash
id sakil           # UID, GID, সব গ্রুপ
sudo chage -l sakil   # এজিং পলিসি
```

**ধাপ ৭ — টেস্ট করা:**

```bash
su - sakil
```

### 👤 ইউজার চলে গেলে (Offboarding):

```bash
sudo passwd -l sakil        # ১. প্রথমে লক করুন (ফাইল রেফারেন্স অটুট থাকে)
# দরকারি ফাইল ব্যাকআপ নিন...
sudo userdel -r sakil       # ২. তারপর সম্পূর্ণ ডিলিট
```

### ❌ কমন ভুলগুলো (অবশ্যই এড়াবেন):

| ❌ ভুল | ফলাফল | ✅ সমাধান |
|--------|---------|-----------|
| `useradd sakil` (`-m` ছাড়া) | হোম ডিরেক্টরি নেই → লগইন সমস্যা | `useradd -m ...` |
| `usermod -G developers sakil` (`-a` ছাড়া) | আগের সব গ্রুপ মুছে যায় (sudo হারাতে পারে!) | `usermod -aG ...` |
| `nano /etc/passwd` (ম্যানুয়াল এডিট) | এক ক্যারেক্টার ভুলে সিস্টেম লগইন ভেঙে যেতে পারে | সবসময় কমান্ড ব্যবহার করুন |
| `su sakil` (হাইফেন ছাড়া) | এনভায়রনমেন্ট লোড হয় না | `su - sakil` |
| `nano /etc/sudoers` (সরাসরি এডিট) | সিনট্যাক্স ভুলে sudo সম্পূর্ণ বন্ধ হয়ে যেতে পারে | `sudo visudo` |

---

## ১২. অধ্যায় সারসংক্ষেপ

### 📝 এক নজরে পুরো বিষয়:

```
ইউজার তৈরি   → useradd -m, passwd, userdel -r
গ্রুপ         → groupadd, groupdel, usermod -aG
তথ্য ফাইল     → /etc/passwd, /etc/shadow, /etc/group, /etc/gshadow
ডিফল্ট সেটিংস → /etc/default/useradd, /etc/login.defs, /etc/skel
পারমিশন      → chmod (755, 644, 600), chown, chgrp
অ্যাডমিন      → sudo, visudo, usermod -aG sudo
নিরাপত্তা     → chage, passwd -l/-u/-e, find -perm -4000
বিশেষ বিট    → SUID(4), SGID(2), Sticky(1)
```

### 🧠 মেন্টাল মডেল (মনে রাখার সহজ উপায়):

যখন আপনি `useradd + passwd + usermod` চালান আপনি আসলে পরোক্ষভাবে **তিনটি ফাইল মডিফাই করছেন**:

| ফাইল | কী যোগ/বদলায় |
|---|---|
| `/etc/passwd` | ইউজারনেম, UID, GID, হোম, শেল |
| `/etc/shadow` | পাসওয়ার্ড হ্যাশ, এজিং |
| `/etc/group` গ্রুপ মেম্বারশিপ |

### 📌 অধ্যায়ের ৫টি গোল্ডেন রুল:

1. ইউজার তৈরিতে সবসময় `-m -s /bin/bash` ব্যবহার করুন
2. গ্রুপে যোগে সবসময় `-aG` (কখনোই শুধু `-G` নয়!)
3. কনফিগ ফাইল ম্যানুয়ালি এডিট নয় — কমান্ড ব্যবহার করুন
4. sudoers এডিট করতে **শুধু** `visudo`
5. root দিয়ে কাজ নয় — sudo দিয়ে কাজ করুন (অডিট ট্রেইল থাকে)

### ✅ নিজে যাচাই করুন (Practice Lab):

```bash
# ১. "testuser" নামে প্রোডাকশন-রেডি ইউজার বানান (হোম + bash + কমেন্টসহ)
# ২. "trainees" গ্রুপ বানিয়ে তাকে যোগ করুন
# ৩. তার পাসওয়ার্ড এজিং সেট করুন (min 5, max 90, warn 7)
# ৪. অ্যাকাউন্ট ৩ মাস পরে এক্সপায়ার করুন
# ৫. অ্যাকাউন্ট লক করুন, তারপর আনলক করুন
# ৬. একটি টিম ফোল্ডার বানিয়ে SGID সেট করুন
# ৭. সবশেষে ইউজারকে হোমসহ সম্পূর্ণ ডিলিট করুন
```

---

## পরবর্তী অধ্যায়ে যা শিখবেন:

* **UMask** — নতুন ফাইলের ডিফল্ট পারমিশন কীভাবে নির্ধারিত হয়
* **ACL (Access Control Lists)** — উন্নত ও নমনীয় পারমিশন ব্যবস্থা (`setfacl`, `getfacl`)
* **সিস্টেম অডিট** — বিপজ্জনক SUID ফাইল খুঁজে বের করা ও নিরাপত্তা যাচাই
* **SSH ও ইউজার** — `authorized_keys` দিয়ে পাসওয়ার্ডহীন নিরাপদ লগইন
* **Docker ও ইউজার** — কন্টেইনারে ইউজার পারমিশন (DevOps দৃষ্টিকোণ)

---

**অভিনন্দন!** এই অধ্যায় শেষ করে আপনি এখন লিনাক্সে ইউজার ম্যানেজমেন্টের সম্পূর্ণ লাইফসাইকেল — তৈরি থেকে নিরাপত্তা পর্যন্ত — আত্মবিশ্বাসের সাথে হ্যান্ডেল করতে পারবেন। Practice Lab-এর কাজগুলো নিজে হাতে করলে বিষয়গুলো দীর্ঘদিন মনে থাকবে!

>  [🏠](../) [⬅️ ০১। লিনাক্স পরিচিতি](../০১-লিনাক্স-পরিচিতি)  [➡️ ০২। শেল এক্সপ্যানশন](../০২-শেল-এক্সপ্যানশন)
